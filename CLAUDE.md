# 十一记账 (ShiYi Accounting)

> 一款运行在 Windows 和 Mac 电脑上的个人记账应用，帮助用户记录每一笔人民币花销。

---

## 一、核心协作原则（极其重要，全项目周期必须遵守）

**用户是编程小白，首次使用 Claude Code，不懂任何编程技术。**

因此，在整个项目开发过程中，我（Claude）必须严格遵守以下规则：

1. **任何技术相关的事情，一律由我列出多个可行方案，向用户解释清楚每个方案是什么、有什么优点和缺点，然后由用户来做最终决定。** 用户无法主动提出技术需求。
2. 涉及技术选型、库的选择、架构设计、数据结构设计、界面布局方式等一切技术决策时，**必须先解释、后决定，绝不替用户拍板**。
3. 解释技术问题时，**必须用通俗易懂的语言**，避免堆砌专业术语；如果必须使用术语，要附带白话解释。
4. 推荐方案时，要说明「我为什么推荐这个」，让用户理解理由后再选择。
5. 每个阶段开始前，先向用户说明「这个阶段要做什么、有哪些选择」，等用户确认后再动手。
6. 用户没有明确要求的技术细节，默认用最稳妥、最简单、最容易维护的方案，并向用户说明。

> ⚠️ 如果我违反了上述原则（例如擅自做了技术决策、或用用户看不懂的话解释），请用户直接指出，我立即改正。

---

## 二、产品简介

- **产品名称**：十一记账
- **一句话介绍**：简单易用的个人记账工具，帮你清清楚楚记录每一笔花销。
- **目标用户**：想记账但不想用复杂财务软件的个人用户。
- **运行平台**：Windows、Mac 电脑（桌面应用）。

---

## 三、核心功能需求

### 1. 记一笔花销（核心功能）
用户能快速记录一笔支出，每条记录包含以下信息：
- **金额**：花销金额（人民币，单位：元，支持小数，如 18.50）
- **分类**：两级分类（先选一级大类，再选二级小类，见下方分类表）
- **日期**：花销日期（默认当天，可修改）
- **备注**：可选，简单文字说明（如「和同事聚餐」）

### 2. 查看账单 / 明细
- 按时间查看花销明细列表
- 可以按「分类」「日期范围」筛选
- 可以统计：某段时间总共花了多少钱

### 3. 分类管理（两级分类）
- **一级大类**：预设若干大类（见下方分类表）
- **二级小类**：每个大类下预设若干小类
- 允许用户自行增删改分类（自定义分类）

### 4. 数据存储
- 数据保存在**本机**，不联网、不上传，保护隐私
- 数据要能长期保存，重启电脑后数据不丢失

### 5. 未来可扩展（暂不实现，仅预留）
- 记录收入（目前只做支出）
- 图表统计（饼图、柱状图等）
- 导出数据（Excel / CSV）

---

## 四、分类体系设计（一级大类 + 二级小类）

> 以下为预设分类，用户后续可自行增删改。

| 一级大类 | 二级小类 |
|---------|---------|
| 🍜 餐饮 | 早餐、午餐、晚餐、外卖、零食饮料、咖啡奶茶、聚餐请客 |
| 🚗 交通出行 | 公交地铁、出租车/网约车、火车高铁、飞机、加油、停车费、共享单车 |
| 🛍️ 购物消费 | 服饰鞋包、日用百货、数码家电、美妆护肤、书籍文具 |
| 🏠 居住生活 | 房租、水电燃气、物业费、宽带网络、家居装修 |
| 🎮 娱乐休闲 | 电影演出、游戏、运动健身、旅游度假、会员订阅 |
| 💊 医疗健康 | 门诊就医、药品、体检、口腔牙科 |
| 📚 学习教育 | 学费、培训课程、书籍资料、考试报名 |
| 🎁 人情往来 | 礼物红包、孝敬长辈、请客随礼 |
| 📱 通讯网络 | 手机话费、流量充值 |
| 📦 其他 | 其他 |

---

## 五、技术选型（✅ 已全部确定）

| 项目 | 选择 |
|------|------|
| 桌面框架 | Electron |
| 界面技术 | React |
| 开发语言 | TypeScript |
| 界面组件库 | Ant Design（会调主题配色，让界面更清新） |
| 数据存储 | SQLite（本机轻量数据库，数据保存在本机，不联网、不上传） |

> 以上均在 2026-08-30 由用户确认选择。

---

## 六、开发路线图与当前进度

开发分 6 个阶段，**每个阶段做完会给你看效果，确认后再进入下一阶段**：

1. **搭建项目骨架**：初始化 Electron + React + TypeScript + Ant Design + SQLite，能启动运行看到界面
2. **数据层**：设计数据库结构，把分类数据初始化进去
3. **记一笔花销**：核心功能——填金额、选分类、选日期、写备注，保存
4. **查看账单 / 明细**：账单列表 + 按分类/日期筛选 + 总额统计
5. **分类管理**：增删改一级/二级分类
6. **测试与打包**：生成 Windows 和 Mac 的可安装程序

- [x] 产品文档与协作原则已写入 CLAUDE.md
- [x] 确定技术选型（Electron + React + TypeScript + Ant Design + SQLite）
- [x] 阶段 1：搭建项目骨架
- [x] 阶段 2：数据层
- [x] 阶段 3：记一笔花销
- [x] 阶段 4：查看账单 / 明细
- [x] 阶段 5：分类管理
- [x] 阶段 6：测试与打包（Windows 安装程序已生成）

---

## 七、开发注意事项（给 Claude / 开发者看）

### 如何启动
- 在项目根目录运行 `npm run dev`（启动后会自动弹出应用窗口），关闭窗口即停止。

### 已知环境坑：ELECTRON_RUN_AS_NODE
- Claude Code 的终端会话会注入环境变量 `ELECTRON_RUN_AS_NODE=1`，这会让 Electron 被当成 Node.js 运行，导致应用启动时报 `electron.app is undefined`。
- 这是会话临时注入，**不是 Windows 系统问题**，用户在自己的普通终端里运行完全不受影响。
- 在 Claude Code 里启动/测试应用时，需临时去掉该变量（Windows Git Bash 下）：
  `env -u ELECTRON_RUN_AS_NODE npm run dev`

### 常用命令
- `npm run dev`：启动开发模式（界面热更新）
- `npm run typecheck`：类型检查
- `npm run build`：构建生产版本
- `npm run build:win`：打包 Windows 安装程序（产物在 `release/` 目录）
- `npm run build:mac`：打包 Mac 安装程序（**必须在 Mac 电脑上运行**）

### 打包说明
- 打包工具：electron-builder，配置见 `electron-builder.yml`
- Windows 产物：`release/ShiyiJiZhang-<版本>-Setup.exe`
- Mac 产物：`release/ShiyiJiZhang-<版本>.dmg`（需在 macOS 上执行 `npm run build:mac`）
- 暂未设置应用图标（用的是 Electron 默认图标），后续可补充
- 在境内打包时，如遇到下载慢/失败，可加镜像环境变量：
  `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/ ELECTRON_BUILDER_BINARIES_MIRROR=https://npmmirror.com/mirrors/electron-builder-binaries/`
